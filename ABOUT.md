# 关于 Youqian PingView (About Youqian PingView)

## 📌 项目概述

**Youqian PingView** 是一款专为 **Linux 与国产操作系统（如统信 UOS Desktop、Deepin、Ubuntu、Debian 等）** 打造的原生图形化多目标批量网络探测与连通性实时监控工具。

本项目深度复刻并致敬了 Windows 平台上由 NirSoft 开发的经典网络运维利器 **PingInfoView**，不仅完整还原了其双窗格大盘联动、高并发免 root 探测、连续故障告警等核心精髓，还针对现代 Linux 桌面环境进行了大量底层架构升级与功能增强。

---

## 💡 为什么开发 Youqian PingView？

随着统信 UOS 等国产 Linux 操作系统的普及，大量企业网管、运维工程师与桌面用户从 Windows 平台迁移到统信 UOS 后，发现 Windows 下经典的绿色网络工具（如 `PingInfoView.exe`）无法在 Linux 原生环境下稳定运行。虽然可以通过 Wine 模拟层运行，但普遍存在 ICMP 原始套接字权限受限、界面字体模糊模糊、快捷键失灵等水土不服的问题。

为了让用户在统信 UOS 上获得如同使用原生应用一般的顺滑体验，**Youqian PingView** 应运而生：
- **纯原生体验**：深度适配统信 DDE 桌面规范与 Wayland/X11 显示环境；
- **双引擎零依赖架构**：系统装有 PyQt5 时提供现代高清水墨风格大盘；在纯内网或未激活系统下自动启用零依赖原生轻量引擎，真正做到拷贝即用、双击秒开；
- **100% 原生 IPv6 深度支持**：完美抓取 IPv6 独有的 `HLIM` 跳数限制与微秒延迟。

---

## 🌟 核心特性与亮点

1. **经典双窗格（上下分栏）设计**：
   - **上窗格（大盘列表）**：实时多目标监控大盘，红绿状态灯直观指示存活，异常主机浅红底色醒目高亮，支持丢包率、延迟波动、TTL/HLIM 全维度统计；
   - **下窗格（流水明细）**：在上窗格点击选中任意主机，下窗格自动同步滚动显示该主机的逐次探测明细日志，所有列宽支持鼠标自由拖拽缩放。
2. **多语法智能解析**：
   - 支持 IP 与域名批量录入；
   - 支持 `192.168.1.1 核心网关` 等 IP+中文描述混排格式；
   - 支持 **CIDR 子网段批量自动展开**（如 `192.168.1.0/24 财务部`）；
   - 支持 **连续 IP 范围批量展开**（如 `192.168.1.1-192.168.1.50`）；
   - 支持 **TCP 端口连通性探测**（如 `192.168.1.100:80`、`[2400:3200::1]:443`）。
3. **人性化多选与表格导出**：
   - 支持按住 `Ctrl` 任意跳选（如单独选择 1、3、5 行）；
   - 支持按住 `Shift` 范围连续选；
   - `Ctrl + C` / 右键复制整行带表头标题，直接粘贴至 Excel 或记事本完美对齐；
   - 支持一键导出可视化自适应 HTML 统计报表与 CSV 表格。
4. **活动故障实时呈现**：
   - 在「仅显示失败/异常」模式下，后台一旦探测到新故障，毫秒级即时弹现在页面上；
   - 支持右键「禁用此项探测」，禁用后立即从失败列表中隐藏。
5. **1:1 原版经典属性面板**：
   - 鼠标双击或右键「属性 (Alt+Enter)」，独立对话框一览 20+ 项参数指标。

---

## 🛠️ 技术架构

- **编程语言**：Python 3 (兼容 Python 3.7 - 3.12+)
- **GUI 引擎**：PyQt5 (优先) / Python 原生 Tkinter (无网络/免依赖保障)
- **底层探测**：异步子进程池并发 ICMP 探测 (100% 免 root 权限) + Socket TCP 双栈探测
- **打包规范**：Debian / 统信 UOS 官方标准 `.deb` 安装包 + Linux 绿色便携包

---

## 👤 作者与贡献

- **主创开发者**：MoMo (ThousyMo)
- **开源代码仓库**：[https://github.com/thousy/Youqian-PingView](https://github.com/thousy/Youqian-PingView)
- **问题反馈与建议**：欢迎在 GitHub 提交 [Issues](https://github.com/thousy/Youqian-PingView/issues) 或 Pull Requests。

---

## 📜 开源协议

本项目基于 [MIT License](https://opensource.org/licenses/MIT) 开源，允许自由使用、分发与二次开发。
特别致敬 [NirSoft](https://www.nirsoft.net/) 及原版 PingInfoView 工具带来的设计灵感！
